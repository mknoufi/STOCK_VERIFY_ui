// Placeholder for generated API types
export type components = {
  schemas: {
    // Add dummy schemas if needed by consumers
    User: {
      id: string;
      username: string;
    };
    Error: {
      code: number;
      message: string;
    };
  };
};

export type paths = Record<string, unknown>;
